# Server-only values. Run npm run demo:setup to copy the backend admin token
# and generate a private demo passcode without printing either credential.
BACKEND_API_URL=http://127.0.0.1:8000/api/v1
ADMIN_API_TOKEN=
DEMO_ADMIN_PASSWORD=
